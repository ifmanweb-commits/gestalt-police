Если в сообщения сообщества пишут сообщение, которое начинается с "#вопрос" - нужно:
1) отрезать команду #вопрос от текста вопроса
2) Собрать данные о пользователе, задавшем вопрос
3) Занести вопрос в БД согласно структуре
4) Получить айди чата экспертов (в конфиге нужно новое поле, кроме айди суперпользователя типа experts_chat_id
5) В этот чат экспертов отправить сообщения: "Вопрос экспертам:" и с новой строки - текст вопроса.
Для этого необходимо использовать функции работы с вопросами, которые нужно разместить в новом модуле questions.py

{
  "superuser_id": 260509440,
  "experts_chat_id": 2000000374
}

2. Новый модуль questions.py
2.1 Функции работы с БД
Функция	Описание
init_questions_db()	Проверяет существование questions.json, создаёт {"_default": {}} если нет
get_questions_db()	Возвращает TinyDB экземпляр
add_question(user_id, user_name, question_text, user_link)	Добавляет вопрос, возвращает question_id
2.2 Структура вопроса в БД
json
{
  "_default": {
    "1": {
      "id": 1,
      "user_id": 123456789,
      "user_name": "Анна Петрова",
      "user_link": "https://vk.com/id123456789",
      "question_text": "Как работать с сопротивлением?",
      "timestamp": "2026-03-20T18:30:00",
      "post_id": null,
      "expert_answers": []
    }
  }
}

2.3 Вспомогательные функции
Функция	Описание
get_next_question_id()	Возвращает следующий ID (max + 1)
format_question_for_experts(question_data)	Форматирует вопрос для отправки экспертам

3. Изменения в vk_bot.py
3.1 Импорт
python
from questions import init_questions_db, add_question, format_question_for_experts
3.2 Загрузка experts_chat_id
В начале файла (после load_dotenv()) добавить

EXPERTS_CHAT_ID = None

def load_experts_chat_id():
    global EXPERTS_CHAT_ID
    try:
        with open(CONFIG_FILE, "r") as f:
            config = json.load(f)
            EXPERTS_CHAT_ID = config.get("experts_chat_id")
    except Exception as e:
        logging.error(f"Не удалось загрузить experts_chat_id: {e}")
Вызывать в main() после load_config().

3.3 Обработчик сообщений в личку
Добавить новый обработчик выше эхо-обработчика:

python
@bot.on.message(IsPrivateRule())
async def handle_question(message):
    """Обработка сообщений, начинающихся с #вопрос"""
    text = message.text
    if not text or not text.startswith('#вопрос'):
        return
    
    # Отрезаем команду
    question_text = text.replace('#вопрос', '', 1).strip()
    
    if not question_text:
        await message.answer("Вы не задали вопрос. Напишите: #вопрос Ваш вопрос")
        return
    
    # Получаем данные о пользователе
    user_id = message.from_id
    user_name = f"user{user_id}"  # Временно, позже можно через API
    user_link = f"https://vk.com/id{user_id}"
    
    # Сохраняем в БД
    question_id = add_question(user_id, user_name, question_text, user_link)
    
    # Отправляем экспертам
    if EXPERTS_CHAT_ID:
        expert_message = f"📩 **Новый вопрос экспертам**\n\n{question_text}"
        await api.messages.send(
            peer_id=EXPERTS_CHAT_ID,
            message=expert_message,
            random_id=random.randint(1, 2**31)
        )
    
    # Ответ пользователю
    await message.answer("✅ Ваш вопрос отправлен экспертам. Ответ придет в личные сообщения.")

    6. Дополнительно (для агента)
questions.json должен создаваться с {"_default": {}} аналогично experts.json

В init-files.sh добавить создание questions.json с правильным форматом

Все асинхронные вызовы await

ID вопросов автоинкрементные (максимальный существующий + 1)

Если пользователь отправил пустой #вопрос - ответить ему текстом:
"Вы неверно задали вопрос. Напишите #вопрос и дальше - развёрнутый текст вопроса одним большим сообщением."

Добавить в конфиг айди группы "Зона роста" - 236116938